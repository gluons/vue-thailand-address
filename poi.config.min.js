const config = require('./poi.config');

config.filename = {
	js: 'vue-thailand-address.min.js',
	css: 'vue-thailand-address.min.css',
	chunk: '[id].chunk.min.js'
};
config.minimize = true;
config.removeDist = false;

module.exports = config;
