export default {
	methods: {
		onItemSelected({ subdistrict, district, province, zipcode }) {
			subdistrict; // ตำบล/แขวง
			district; // อำเภอ/เขต
			province; // จังหวัด
			zipcode; // รหัสไปรษณีย์
		}
	}
};
